{
  "name": "claudesync",
  "version": "0.1.0",
  "description": "Sync your Claude Code config across machines. Dotfiles for Claude.",
  "type": "module",
  "bin": {
    "claudesync": "./cli/index.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "keywords": ["claude", "claude-code", "dotfiles", "sync", "config"],
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^1.1.0"
  }
}
